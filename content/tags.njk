<h1>Tags</h1>

<ul>
{% for tag in collections | getKeys | filterTagList %}
	{% set tagUrl %}/tags/{{ tag | slugify }}.html{% endset %}
	<li><a href="{{ tagUrl | relativeUrl }}" class="post-tag">{{ tag }}</a> ({{collections[tag].length}} posts)</li>
{% endfor %}
</ul>
